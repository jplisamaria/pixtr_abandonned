<h2><%= @image.name %></h2>
<p> <%= @image.description %> </p>
<nav>
  <%= link_to "Edit Picture" , edit_image_path(@image) %>
  <%= link_to "Delete Picture" , @image, method: :delete, confirm: "Are you sure you want to delete this image?" %>
</nav>
<img src="<%= @image.url %>">

